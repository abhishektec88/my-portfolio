/**
 * Legacy chunk: thumbnail URLs (same export names as original `DABWVKfi.js`).
 * Edit paths in `src/data/legacy/thumbnails.ts` instead.
 */
import { legacyProjectThumbnails as paths } from "../data/legacy/thumbnails";

export const a = paths.clientPortfolio;
export const b = paths.ngoWebsite;
export const c = paths.resumeBuilder;
export const d = paths.ecommerce;
export const e = paths.aiRiskPlatform;
export const f = paths.financialSecurityDashboard;
